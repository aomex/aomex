datasource db {
  provider = "mysql"
  // url      = "DATABASE_URL"
}

generator aomex {
  provider = "tsx ./packages/prisma/src/bin.ts"
  output   = "./table-name.mysql"
}

model lowercase {
  id Int @id @default(autoincrement())
}

model UPPERCASE {
  id Int @id @default(autoincrement())
}

model camelCase {
  id Int @id @default(autoincrement())
}

model PascalCase {
  id Int @id @default(autoincrement())
}

model Titlecase {
  id Int @id @default(autoincrement())
}

model snake_case {
  id Int @id @default(autoincrement())
}

model Complete_Case {
  id Int @id @default(autoincrement())
}
